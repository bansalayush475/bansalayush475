name: Generate Snake

on:
  push:
    branches:
      - main          # runs whenever you push to main
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"   # optional backup daily run

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Generate snake
        uses: Platane/snk@v3
        with:
          github_user_name: bansalayush475
          outputs: |
            dist/github-contribution-grid-snake.svg

      - name: Deploy snake to output branch
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: output
          folder: dist
